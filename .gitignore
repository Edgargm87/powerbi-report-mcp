node_modules/
*.js.map
.usage/
pbi report/

# Build artifacts (rebuilt by `npm install` via prepare script, or `npm run build`)
dist/

# Personal IDE / editor configs
.claude/
.vscode/
.idea/

# Local notes / audit docs not for public consumption
IMPROVEMENTS-FOR-CLAUDE-CODE.md

# Cowork plugin build outputs (rebuild with `npm run plugin:build`)
cowork-plugin-build/
*.plugin

# Eval per-run report (regenerated each `npm run eval`); baseline.md is committed
evals/last-report.md