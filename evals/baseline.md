# Eval Baseline

Run pending — set `ANTHROPIC_API_KEY` and execute `npm run eval` to capture baseline accuracy.

## Latest run (placeholder)

- Accuracy: TBD
- Avg duration: TBD
- Avg tool calls: TBD
